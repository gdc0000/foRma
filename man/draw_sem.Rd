% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/renderer.r
\name{draw_sem}
\alias{draw_sem}
\title{Render the SEM plot from parsed data frames.}
\usage{
draw_sem(json_data)
}
\arguments{
\item{json_data}{A list containing parsed JSON data with 'nodes' and 'edges' elements from the Gemini API response.}
}
\description{
Render the SEM plot from parsed data frames. Creates a publication-ready ggplot with APA-style formatting, proper node shapes (ellipses for latent, rectangles for observed), and significance-based path styling.
}
\examples{
# This function is typically called internally by visualize_sem()
# Example usage with mock data:
json_data <- list(
  nodes = data.frame(
    id = c("visual", "textual", "x1", "x2"),
    label = c("Visual", "Textual", "x1", "x2"),
    x = c(200, 600, 100, 100),
    y = c(300, 300, 200, 400),
    type = c("LATENT", "LATENT", "OBSERVED", "OBSERVED"),
    width = c(120, 120, 80, 80),
    height = c(80, 80, 50, 50)
  ),
  edges = data.frame(
    sourceId = c("visual", "visual", "textual"),
    targetId = c("x1", "x2", "x4"),
    label = c("0.75***", "0.68***", "0.82***"),
    lineType = c("straight", "straight", "straight"),
    isSignificant = c(TRUE, TRUE, TRUE),
    labelPosition = c(0.5, 0.5, 0.5)
  )
)
plot <- draw_sem(json_data)
print(plot)
}
